interface:
  display_name: 'LightReader Essential Verification'
  short_description: 'LightReader 默认只跑基础校验，关键边界改动时按需升级'
  default_prompt: 'Use $lightreader-verify-efficiently to run only the basic and important checks required by this LightReader change.'
